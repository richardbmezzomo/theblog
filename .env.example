# PostgreSQL connection string (Neon recommended)
DATABASE_URL=postgresql://user:password@host/dbname

# Admin panel password (used to log in at /login)
ADMIN_PASSWORD=your_admin_password

# Session encryption secret — use a long random string (min 32 chars)
SESSION_SECRET=your_session_secret_at_least_32_characters_long
